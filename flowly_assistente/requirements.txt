pyttsx3==2.90
requests==2.31.0
python-dotenv==1.0.1
pymongo==4.8.0

# Speech-to-text (Google via SpeechRecognition)
SpeechRecognition==3.10.0

# Python 3.13+: SpeechRecognition ainda importa aifc/audioop; este backport restaura os módulos
standard-aifc==3.13.0; python_version >= '3.13'

# HTTP runtime (Google Cloud Functions/Run via Functions Framework)
functions-framework==3.*

# Background jobs (analytics mining)
celery==5.4.0
redis==5.0.8

# -----------------
# Voz (opcional)
# -----------------
# Captura de microfone (opcional):
# - Via PyAudio (SpeechRecognition.Microphone)
#   pyaudio==0.2.13
# - Ou via SoundDevice (fallback implementado nesta assistente)
#   sounddevice==0.4.7
#   numpy==2.2.6
